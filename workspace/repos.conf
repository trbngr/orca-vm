# Repositories to clone into repos/, one `owner/name` per line. Blank lines and comments are ignored.
# Seeded to ~/<dir>/repos.conf on the box once; later additions go in BOTH places (seeding never
# overwrites), then run ~/<dir>/init.sh.

your-org/your-repo
